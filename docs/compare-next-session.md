# Compare: delegated-work handoff

**Packaging handoff:** [reference-next-session.md](reference-next-session.md) is
the current entrypoint for a fresh coordinator after the local checkpoint. The
working-tree/process descriptions below preserve earlier investigation states.

## Completion update — read before historical dispatch details

The builder, trial lead and fresh author have completed and exited. Read
`compare-reference.md`, `compare-trial.md` and `skills/hud/compare.md` for the actual
result. The PO subsequently said the Compare view is **“way easier to visually
parse”** than the prose baseline. Preserve that demonstrated visual benefit;
do not promote it to factual fidelity, better-decision, production-API or broader
palette approval. The local gate passes80tests; all172protectedSequence fingerprints
remain unchanged.

The author selected Compare for three alternatives packets (one was the supplied
anchor), produced numbered Markdown for the run sheet, and prose for a single
reimbursement. The run sheet demonstrates semantic selection, not a validated or
rendered HUD Sequence invocation. Compare artifacts had real fidelity omissions;
raw evidence remains separate from a corrected archive-services demonstration.

Primary review found missing numeric limits, a complete JSON shape and a Sequence
invocation link in the frozen discovery. The trial lead corrected discovery only
AFTER archiving the original files, with no author rerun. Use the archive/current
manifests described in the trial record; don't check the old live-path manifest
against the intentionally changed entrypoint or imply the correction was trialed.

Current PO demo at capture: owned `w7:pC` shows retained supplier prose as shell
output; owned `w7:pD` runs Compare on `examples/compare-suppliers.json`. Both PTYs
are71x35 (left temporarily set to71columns, physical width72at setup). Only Compare
is interactive. Primary `w7:p1` is focused; completed trial-lead pane `w7:pG` was
exited, shell-verified and closed. No builder/author/reviewer remains active.
The previous Sequence comparison is preserved in evidence, not still running.
See `docs/assets/compare/po-demo/` for the primary's final paired captures.

Everything is still local and uncommitted on `feat/sequence-reference` atc010e7d;
no staging/commit/branch switch/push/merge/install/release/remote-memory write.
Rediscover all pane handles/dimensions before control, especially after resizing.

## Historical dispatch state

## Authority and next checkpoint

Travis explicitly approved Compare after accepting the Sequence visual reference,
then warned the coordinator about approaching compaction. The current approval is
recorded at the top of `semantic-visual-language.md`. Keep him Product Owner;
delegate substantial work to fresh agents and bring demonstrated experience and
consequential tradeoffs, not engineering questionnaires.

Only one new semantic reference is approved. Product question:
**Does Compare make alternatives easier to judge than prose without losing
important facts or forcing an inappropriate visual form?**

Dottie consultation `6eb9eac8-9bc4-4f64-bd58-061d04526d55` recommended this next
experiment but found no prior next-feature commitment in bounded recovered history.
Approval came from Travis afterward. Consultations stay read-only; no remote
memory/project writes, publication, installation, release or broader palette work.

## Active delegation at handoff

- Checkout `/home/tnez/Work/tnezdev/hud`.
- Branch `feat/sequence-reference`, HEAD `c010e7d`.
- Sequence implementation/evidence is still **uncommitted**. Do not reset, stash,
  switch branches or confuse the dirty baseline with new Compare work.
- Fresh pi agent `hud-compare-builder` was started in owned pane `w7:pC` and given
  `/tmp/hud-compare-builder-task.md`. Its remit is a Dottie-grounded repository-local
  brief **before code**, then one isolated data-only Compare example, authoring
  discovery teaching Sequence/Compare/prose selection, validated examples/captures,
  automated coverage and green `./scripts/check`.
- It must preserve accepted Sequence files, old prototypes, production `src/`,
  dependencies and the interval/document WIP; no git operations, new agents/panes,
  fresh-author trial, live demo control or product-acceptance claims.
- A second fresh pi agent, `hud-compare-trial-lead`, was started in new owned
  `w7:pG` with `/tmp/hud-compare-trial-lead-task.md`. It owns bounded independent
  review, freeze, separate fresh-author trial, fidelity/evidence checks and demo
  coordination after builder completion. Builder was told about this delegation.
  The primary retains Product Owner communication and git/checkpoint management.
- The trial lead may gracefully exit/reuse the finished builder pane for a fresh
  author, and reuse our demo pane `w7:pD`; it may create at most one extra sibling
  if genuinely needed. It must preserve primary `w7:p1` focus/cwd and never prompt,
  reset or close the primary. It must not alter unrelated panes or its own reporting
  pane before primary retrieves the final report. Rediscover every live handle.
- The builder still stops after brief/implementation/examples/checks. The trial
  lead must not claim a fresh-author success merely from that implementation.
- Protected Sequence hashes: `/tmp/hud-compare-sequence-baseline.json` (172files at
  dispatch). It covers examples/sequence*, skills/hud/sequence.md, docs/sequence*.md
  and docs/assets/sequence/**. Verify before calling new work complete.

These are historical handles, not authority to target whatever later occupies a
pane. Rediscover live topology, agent identity and foreground processes first.
The baseline demo in `w7:pC` was quit and shell-verified before agent start. Accepted
Sequence remains live in owned `w7:pD`; caller `w7:p1` retained focus/cwd. Do not
assume an unrelated pane still exists merely because earlier handoffs mention it.

## Remain separate from implementation

The fresh author must not be the builder. Freeze the finished discovery/source
before testing; don't leak expected selections or layout answers. Retain the
supplier prose anchor `examples/sequence-suppliers.txt`, add genuinely new material,
and include a case where ordinary prose should win. Assess selection, fidelity,
presentation and actual effort separately. Earlier Sequence trials selected well
but still lost identifier source, explicit USD and fictional provenance; corrected
demos did not establish unassisted author reliability.

No Compare completion, new gate result, independent review or fresh-author outcome
is claimed by dispatching this worker. Sequence's last completed gate was74tests
plus formatting and Clippy. Read the builder's actual report/files, do not infer
success from a settled lifecycle status. If final terminal output is truncated,
request a complete Markdown report in a temporary file rather than treating a
short terminal excerpt as the entire result.

Original context: `sequence-trial.md`, `sequence-rhythm.md`, `sequence-header.md`.
The accepted presentation includes one outer top-padding row, clear title/context
hierarchy, meaningful sequence section labeling, compact item interiors and the
numbered connected spine. Do not redesign that accepted reference to accommodate
Compare. General frameworks and production API promotion remain out of scope.
